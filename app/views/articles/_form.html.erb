<%= form_for([@article,@article.comments.build], remote:true, html: {id: :form_id}) do |f| %>
	<%= f.label :comment%>
	<br/>
	<%= f.text_area :comment, rows:"4",id:"text_content"%>
	<br/>
	<%= f.submit :submit%>
<% end %>
