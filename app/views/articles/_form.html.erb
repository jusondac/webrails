<%= form_for @comment do |f| %>
	<%= f.hidden_field :article_id, value:@article.id%>
	<br/>
	<%= f.label :comment%>
	<br/>
	<%= f.text_area :comment, cols: 50%>
	<br/>
	<%= f.submit :submit%>
<% end %>